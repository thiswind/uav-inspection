# compose 环境变量样例（复制为 .env 后按需修改；本服务无密钥）
# 本机 registry 前缀（insecure 直连最快）；换成自己的 registry 地址即可
UAV_IMAGE=127.0.0.1:15000/uav/inspection:latest
# 反向代理子路径前缀（与 nginx 片段一致；根路径部署置空）
UAV_URL_PREFIX=/uav
# 宿主侧回环端口（nginx upstream 对准它；直连访问时可改绑 0.0.0.0）
UAV_PORT=18020
