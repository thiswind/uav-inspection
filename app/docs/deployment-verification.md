# 部署验收记录

验证日期：2026-09-03。实际环境：Windows、Python 3.12、Node.js 24。

- 新建独立 `.venv-deploy`，从 `requirements.txt` 联网安装基础依赖成功，`pip check` 通过。
- 确认该环境未安装 `torch` 和 `ultralytics`；指向全新空数据目录后启动成功。
- 实际 HTTP 验证：健康检查、视频列表、测量概览、预测、遥测等基础接口返回 200；测量/视频返回空数据。
- 根目录 14 项打包和启动配置测试通过（其中 Windows 符号链接权限测试跳过 1 项）。分卷大小上限、校验、拼接与防覆盖都有测试。
- 后端 9 项部署测试通过，覆盖缺数据/模型/依赖/MongoDB、上传、静态视频、4 条实时通道、12 个页面深链接、路径边界与外置图片。
- 独立无数据程序副本通过 `npm ci` 和 `npm run build`；未依赖原来的 `node_modules`。
- 浏览器检查首页、热力图、外墙、乔木高度、绿化面积和玫瑰页面；修复并复验生产构建地图插件导致的空白页，验证空测量提示和缺照片占位。

已验证基础部署，不代表全部算法精度验收。真实 AI 识别、CUDA/GPU、FFmpeg 转码、无人机硬件、WebODM、离线安装和非 Windows 环境没有做完整端到端验收。原有外部底图仍需要联网。

打包过程保留原始文件。交付不含本机依赖、旧构建及训练历史，依赖需在接收方首次联网安装。
